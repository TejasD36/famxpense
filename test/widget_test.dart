import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — app requires Firebase setup which is not available in tests.
    // Replace with meaningful widget tests once Firebase mocks are configured.
    expect(1 + 1, equals(2));
  });
}
